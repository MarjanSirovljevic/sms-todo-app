import React from 'react';

const footer = {
  background: 'yellow',
  width: '100%',
  position: 'fixed',
  bottom: 0,
  zIndex: 1000,
};

const Footer = () => (
  <footer style={footer}>
    Footer Component
  </footer>
);

export default Footer;
